# v1 — Basic RAG

Fixed-size chunks + retrieval placeholder. Baseline for the scorecard.

## Status

Runnable stub (keyword scoring stands in for embeddings / LLM).

## Run

```bash
python modules/enterprise-rag/cli.py ask --version v1_basic_rag --question "When did the California laptop promotion end?"
```
