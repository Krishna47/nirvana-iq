# Benchmarks

Latency, cost, and scale-corpus load tests live here.

## Planned

- Gold eval latency (p50 / p95) per pipeline version
- Index build time for gold vs scale
- Cost per 100 questions (embed + generate)

Run notes and CSV dumps go in this folder (gitkeep large artifacts out of git).
